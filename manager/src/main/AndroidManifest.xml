<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- File access for APK processing -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <!-- Install/uninstall packages -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />
    <!-- Internet for rule downloads -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Query all installed packages (Android 11+) -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="AdSweep Manager"
        android:largeHeap="true"
        android:theme="@style/Theme.AdSweep">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AppListActivity" />

        <receiver android:name=".CommandReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.adsweep.manager.CMD_SELECT" />
                <action android:name="com.adsweep.manager.CMD_PATCH" />
                <action android:name="com.adsweep.manager.CMD_UNINSTALL" />
                <action android:name="com.adsweep.manager.CMD_INSTALL" />
                <action android:name="com.adsweep.manager.CMD_STATUS" />
            </intent-filter>
        </receiver>

        <receiver android:name=".InstallReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.adsweep.manager.INSTALL_STATUS" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
